{
  "name": "otg-hmrc-api",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test:aws": "vitest run --config vitest.aws.config.ts",
    "test:mocked": "vitest run --config vitest.mocked.config.ts",
    "test:all": "vitest run",
    "deploy": "../deploy.sh",
    "compile": "tsc"
  },
  "devDependencies": {
    "@aws-sdk/client-cloudformation": "3.1012.0",
    "@aws-sdk/lib-dynamodb": "3.1012.0",
    "testcontainers": "11.12.0",
    "vitest": "4.1.5"
  }
}

{
  "name": "secrets-manager-handler",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "unit": "jest --silent",
    "test": "npm run unit --",
    "test:coverage": "npm run unit -- --coverage",
    "deploy": "../../deploy.sh",
    "compile": "tsc"
  },
  "dependencies": {
    "@aws-lambda-powertools/parameters": "^2.8.0",
    "@aws-sdk/client-secrets-manager": "^3.549.0",
    "aws-sdk-client-mock": "^4.0.0"
  }
}
